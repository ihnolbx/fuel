{
  "name": "fuel-sync-worker",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "wrangler": "^4.105.0"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply fuel --local",
    "migrate:remote": "wrangler d1 migrations apply fuel --remote"
  }
}
